<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= news.title %> - Ezan Vakfı</title>
    <link rel="icon" href="/assets/images/favicon.png">
    <!-- CSS only -->
    <link rel="stylesheet" type="text/css" href="/assets/css/bootstrap.min.css"> 
    <!-- fancybox -->
    <link rel="stylesheet" href="/assets/css/jquery.fancybox.min.css">
    <link rel="stylesheet" href="/assets/css/nice-select.css">
    <!-- Font Awesome 6 -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- style -->
    <link rel="stylesheet" href="/assets/css/style.css">
    <!-- responsive -->
    <link rel="stylesheet" href="/assets/css/responsive.css">
    <script src="/assets/js/jquery-3.6.0.min.js"></script>
    <script src="/assets/js/preloader.js"></script>
</head>
<body>
    <!-- preloader -->
    <div class="preloader">
        <div class="loader">
            <div class="dot"></div>
            <div class="dot"></div>
            <div class="dot"></div>
            <div class="dot"></div>
        </div>
    </div>
    <!-- preloader end -->
    
  <!-- Header partial -->
  <%- include('./partials/site/header') %>
  
    <section class="bannr-section" style="background-image: url(/assets/images/bg/bg.jpg); min-height: 300px; background-size: cover; position: relative; display: flex; align-items: center; justify-content: center;">
        <div class="container">
        <div class="bannr-text" style="display: flex; flex-direction: column; align-items: center; justify-content: center; width: 100%; text-align: center;">
            <h2 style="margin-bottom: 20px; z-index: 2; color: #fff;"><%= news.title %></h2>
            <img src="/assets/images/heading-img.png" alt="icon" width="100" height="132.5" style="z-index: 1;">
                        </div>
                    </div>
    </section>
    <section class="py-5">
        <div class="container">
            <div class="row">
                <div class="col-lg-8">
                    <div class="blog-two our-blog hoverimg">
                        <div class="d-flex align-items-center">
                            <span><%= news.category %></span>
                            <h6><%= new Date(news.createdAt).toLocaleString('tr-TR', { day: 'numeric', month: 'long', year: 'numeric' }) %></h6>
                        </div>
                        <h5 class="mb-2"><%= news.title %></h5>
                        <figure>
                            <img src="<%= news.image %>" class="object-fit-cover" alt="<%= news.title %>" width="851" height="471">
                        </figure>
                        <div class="news-content" style="text-align: justify; width: 100%; overflow-wrap: break-word;">
                            <%- news.content %>
                        </div>
                    </div>
                </div>
                <div class="col-lg-4">
                    <div class="sidebar">
                        <h3>Recent Posts</h3>
                        <div class="boder-bar"></div>
                        <ul class="recent-post">
                            <% if (recentNews && recentNews.length > 0) { %>
                                <% recentNews.forEach(function(item) { %>
                                    <li>
                                        <img alt="<%= item.title %>" src="<%= item.image %>" width="90" height="80" class="object-fit-cover">
                                        <div>
                                            <a href="/news/<%= item.slug %>"><%= item.title %></a>
                                            <span><%= new Date(item.createdAt).toLocaleString('tr-TR', { day: 'numeric', month: 'long', year: 'numeric' }) %></span>
                                        </div>
                                    </li>
                                <% }); %>
                            <% } else { %>
                                <li>
                                    <p>Hozircha yangiliklar yo'q</p>
                                </li>
                            <% } %>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer partial -->
    <%- include('./partials/site/footer') %>
    <!-- search-popup -->
    <div class="search-popup">
        <button class="close-search"><i class="fa-solid fa-xmark"></i></button>
        <form method="post" action="#">
            <div class="form-group">
                <input type="search" name="search-field" value="" placeholder="Search Here" required="">
                <button type="submit"><i class="fa fa-search"></i></button>
            </div>
        </form>
    </div>
    <!-- search-popup end -->
    <!-- progress -->
    <div id="progress">
          <span id="progress-value"><i class="fa-solid fa-up-long"></i></span>
    </div>
    <!-- Bootstrap Js -->
    <script src="/assets/js/bootstrap.min.js"></script>
    <script src="/assets/js/jquery.nice-select.min.js"></script>
    <script src="/assets/js/jquery.fancybox.min.js"></script>
    <script src="/assets/js/custom.js"></script>
</body>